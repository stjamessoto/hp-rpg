{
  "name": "hp-rpg",
  "private": true,
  "version": "0.1.0",
  "description": "Text-based Harry Potter RPG (v1, browser) with a portable core for future mobile port.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run typecheck && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "npm run typecheck:core && npm run typecheck:web && npm run typecheck:scripts",
    "typecheck:core": "tsc --noEmit -p core/tsconfig.json",
    "typecheck:web": "tsc --noEmit -p web/tsconfig.json",
    "typecheck:scripts": "tsc --noEmit -p scripts/tsconfig.json",
    "ingest": "tsx scripts/ingest-canon.ts",
    "ingest:offline": "tsx scripts/ingest-canon.ts -- --offline",
    "desktop:dev": "npm run dev --prefix desktop",
    "desktop:start": "npm run build && npm run start --prefix desktop",
    "desktop:dist": "npm run build && npm run dist --prefix desktop"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.16.0",
    "typescript": "^5.5.0",
    "vite": "^5.3.0",
    "vitest": "^2.0.0"
  }
}
